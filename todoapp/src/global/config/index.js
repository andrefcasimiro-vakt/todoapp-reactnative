/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 *
 * @format
 * @flow
 */

type Config = {
  APP: {
    NAME: string,
    VERSION: number,
  }
}

const config: Config = {
  APP: {
    NAME: '',
    VERSION: 0.1,
  },
}

export default config;
